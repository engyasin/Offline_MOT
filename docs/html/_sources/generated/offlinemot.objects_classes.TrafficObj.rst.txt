offlinemot.objects\_classes.TrafficObj
======================================

.. currentmodule:: offlinemot.objects_classes

.. autoclass:: TrafficObj

   
   
   

   
   
   .. HACK -- the point here is that we don't want this to appear in the output, but the autosummary should still generate the pages.
      .. autosummary::
         :toctree:
      
         TrafficObj.draw
         TrafficObj.filter_by_bg_objs
         TrafficObj.filter_by_detections_dist
         TrafficObj.find_center
         TrafficObj.find_true_size
         TrafficObj.get_detection_format
         TrafficObj.re_init_tracker
         TrafficObj.set_bg_substract
         TrafficObj.set_detection
         TrafficObj.set_track_id
         TrafficObj.track
         TrafficObj.update
   
   